# 已确认决策.md

> 用户已确认的决策摘要。完整决策见 `D:\Projects\SAAS\docs\决策\ADR-*.md`。

## V1 范围优先级

| 决策 | 主源 | 当前口径 |
|---|---|---|
| V1 范围优先级 | ADR-002 | 旧 V2.2 与 V1 冲突时，以 V1 范围和当前代码证据为准 |
| 模块化单体 | ADR-001 | 当前不是 FastAPI / Celery 旧方案 |
| 订单域边界 | ADR-003 | 订单域只存事实，不算提成 / 最终归属 |
| 业绩域边界 | ADR-004 | 最终归属、提成、冲正属业绩域 |
| 分析模块边界 | ADR-005 | 分析模块只读汇总，不重算业绩 |
| real-pre 环境 | ADR-006 | real-pre 是真实上游 / 生产形态联调环境 |
| 活动列表入口路由 | ADR-007 | 统一入口 |
| 活动商品上游推广中状态 | ADR-008 | 上游推广中状态优先 |

## DDD 增量合并决策（2026-06-02）

- 本项目已有 Harness，本次**采用增量合并**，不重建 Harness
- DDD 优化顺序：用户域 → 配置域 → 订单域 → 业绩域 → 分析模块 → 商品域 → 达人域 → 寄样域 → Outbox 事件 → 前端领域化 → E2E 验收与垃圾回收
- V1 使用 Spring Boot + PostgreSQL + Redis + Docker Compose，**不切换 FastAPI**
- V1 采用**模块化单体**，不拆微服务
- V1 订单域只存事实，不计算提成
- V1 业绩域负责最终归属、提成、冲正、汇总刷新
- V1 分析模块只读汇总，不重新计算归因
- V1 不启用独家达人、独家商家、个别品负责人覆盖

## 用户域 dept_type 口径

`sys_dept.dept_type` Java / seed 标准统一为：
- `department`
- `recruiter_group`
- `channel_group`
- `ops_group`

旧 `recruiter / channel / dept` 仅允许出现在 migration 兼容映射中。

## 用户 2026-06-05 决策：毛利纳入 V1

- 经营毛利口径扩展纳入 V1 验收（**P0-004 撤销**）
- 不扩展为财务结算口径
- 前端展示补齐任务

## 用户 2026-06-12 决策：第二大脑中文版

- 在 vault 根下新建 `第二大脑/` 目录
- 改为结构约束与索引覆盖口径
- 中文目录 + 中文文件名为主
- 旧 3 套知识库（LLM Wiki/Harness/、09-SaaS体系/、团长SaaS知识库/）作为归档源保留不动

## 新决策写入规则

1. 先收集源码、接口、数据、日志或团队输入证据
2. 涉及业务规则或架构边界时，在 `docs/决策/ADR-*.md` 新建 ADR
3. 同步追加到本文件 + 业务相关 `03-领域知识/*.md`
4. 涉及 harness 规则 → 同步到 `harness/manifests/gc/*.md`
