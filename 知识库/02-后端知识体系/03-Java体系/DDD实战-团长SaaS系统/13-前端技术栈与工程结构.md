---
title: 13-前端技术栈与工程结构
tags: [Vue3, Pinia, Axios, 前端工程化]
created: 2026-05-10
updated: 2026-05-10
sources: [frontend/src/]
---

# 13-前端技术栈与工程结构

## 1. 技术栈

| 层级 | 技术选型 |
|---|---|
| 框架 | Vue 3 |
| 状态管理 | Pinia |
| HTTP 客户端 | Axios |
| 构建工具 | Vite |
| UI 框架 | 未使用主流 UI 库（自定义样式） |
| CSS | 样式目录 + scoped |
| API 风格 | RESTful JSON |

## 2. 目录结构

```
frontend/src/
├── api/           # Axios 封装接口，按模块分组
├── assets/        # 静态资源（图片/字体）
├── components/    # 公共组件（全局复用）
├── composables/   # Vue 3 Composition API 复用逻辑
├── constants/     # 常量定义
├── router/        # Vue Router 路由配置
├── stores/        # Pinia 状态管理
├── styles/        # 全局样式
├── types/         # TypeScript 类型定义
├── utils/         # 工具函数
└── views/         # 页面组件
    ├── dashboard/  # 工作台/首页
    ├── data/       # 数据统计
    ├── dev/        # 开发调试页
    ├── layout/     # 布局组件
    ├── ops/        # 运营管理
    ├── orders/     # 订单管理
    └── product/    # 商品管理
```

## 3. 路由结构（views）

根据目录推断的页面分组：

| 路径前缀 | 说明 |
|---|---|
| `/dashboard` | 工作台首页 |
| `/data` | 数据统计/分析 |
| `/orders` | 订单管理 |
| `/product` | 商品管理 |
| `/ops` | 运营管理（达人/样品） |

## 4. API 层（api/）

Axios 封装模式，按模块调用后端 REST 接口：

```typescript
// api/request.ts（推断）
import axios from 'axios'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10_000,
})

// 请求拦截：注入 JWT token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截：统一错误处理
request.interceptors.response.use(
  response => response.data,
  error => {
    // 401 无权限 → 跳转登录
    // 400 业务异常 → 提示 message
    // 500 服务器异常 → 提示 message
  }
)
```

## 5. 状态管理（stores/）

Pinia store，按业务域划分：

- `useUserStore` — 用户信息、登录状态
- `usePermissionStore` — 权限、角色
- （其他 store 按需）

## 6. 已知约束

| 约束 | 说明 |
|---|---|
| 无 TypeScript 类型推断 | 若未启用 TypeScript，所有 API 响应为 `any` |
| 无全局 UI 库 | 样式独立维护，组件开发成本较高 |
| 无前端 CI/CD | 部署依赖宝塔面板手动操作 |

## 7. 相关概念

- [[DDD实战-团长SaaS系统/03-本地与三方调用SOP分离]]（后端 API 接口规范）
- [[DDD实战-团长SaaS系统/05-认证授权体系]]（JWT token 和前端鉴权）
