---
title: 19-归因重放与历史订单修复
tags: [归因重放, 历史订单修复, dry-run, safe-update, 幂等]
created: 2026-05-10
updated: 2026-05-10
sources: [OrderAttributionReplayService.java, AttributionService.java, AttributionSourceNormalizer.java]
---

# 19-归因重放与历史订单修复

## 1. 概述

归因重放（replay）用于修复历史未归因订单。当系统升级归因逻辑、修复 Bug、或补充 PickSourceMapping 数据后，需要对历史订单重新执行归因算法。核心安全机制：**dry-run 模式**（先预览不写入）+ **safe-to-update 判断**（避免用"之后才创建"的 mapping 覆盖历史正确归因）。

## 2. 核心入口

```java
// OrderAttributionReplayService.java
@Transactional
public ReplayResult replay(List<String> orderIds, String reason, Integer limit, boolean dryRun)
```

| 参数 | 说明 |
|---|---|
| `orderIds` | 指定订单 ID 列表（null = 按条件查询所有未归因订单） |
| `reason` | 按 `attribution_remark` 过滤（如 `NO_PICK_SOURCE`） |
| `limit` | 单次重放上限（默认 50，最大 200） |
| `dryRun` | true = 只跑归因不写入；false = 执行并写入 |

## 3. 查询要重放的订单

```java
// OrderAttributionReplayService.java:104-119
private List<ColonelsettlementOrder> loadOrders(List<String> orderIds, String reason, Integer limit) {
    LambdaQueryWrapper<ColonelsettlementOrder> wrapper = new LambdaQueryWrapper<ColonelsettlementOrder>()
            .eq(ColonelsettlementOrder::getDeleted, 0);

    if (orderIds != null && !orderIds.isEmpty()) {
        // ① 指定订单 ID 重放
        wrapper.in(ColonelsettlementOrder::getOrderId, orderIds);
    } else {
        // ② 按条件查询所有未归因订单
        wrapper.eq(ColonelsettlementOrder::getAttributionStatus, AttributionService.STATUS_UNATTRIBUTED);
        if (StringUtils.hasText(reason)) {
            wrapper.eq(ColonelsettlementOrder::getAttributionRemark, reason.trim());
        }
        wrapper.last("limit " + normalizeLimit(limit));
    }
    wrapper.orderByDesc(ColonelsettlementOrder::getUpdateTime)
            .orderByDesc(ColonelsettlementOrder::getCreateTime);
    return orderMapper.selectList(wrapper);
}
```

两种模式：**指定订单 ID** 或 **批量扫描未归因订单**。

## 4. 重放核心逻辑

```java
// OrderAttributionReplayService.java:49-86
for (ColonelsettlementOrder order : orders) {
    // 1. 归因
    Map<String, Object> normalizedSource = AttributionSourceNormalizer.normalize(order.getExtraData());
    AttributionService.AttributionResult result = attributionService.resolveAttribution(order, normalizedSource);

    // 2. 统计
    if (AttributionService.STATUS_ATTRIBUTED.equals(result.attributionStatus())) {
        attributed++;
    } else {
        unattributed++;
        stillUnattributed++;
    }

    // 3. 安全检查
    boolean safeForHistoricalUpdate = isSafeForHistoricalUpdate(order, result);

    if (dryRun) continue;  // 干跑不写入

    if (!safeForHistoricalUpdate) continue;  // 不安全就跳过

    // 4. 写入归因结果
    applyAttribution(order, result);
    persistenceService.persistOrder(order);
    updated++;
}
```

## 5. 安全更新判断（isSafeForHistoricalUpdate）

这是 replay 的**最核心**逻辑——防止用"订单创建之后才生成的 mapping"来错误覆盖历史正确归因：

```java
// OrderAttributionReplayService.java:157-169
private boolean isSafeForHistoricalUpdate(ColonelsettlementOrder order, AttributionService.AttributionResult result) {
    // 1. 未归因的可以直接改
    if (!AttributionService.STATUS_ATTRIBUTED.equals(result.attributionStatus())) {
        return false;
    }

    AttributionService.NativeMappingTrace nativeTrace = result.nativeTrace();
    // 2. 非 native 归因的可以直接改
    if (nativeTrace == null || !nativeTrace.nativeKeyMatched()) {
        return true;
    }

    // 3. native 归因：检查 PickSourceMapping 创建时间是否在订单时间之前
    if (nativeTrace.mappingCreatedAt() == null || order.getCreateTime() == null) {
        return false;
    }
    // mapping 在订单之后创建 → 不安全，不更新
    return !nativeTrace.mappingCreatedAt().isAfter(order.getCreateTime());
}
```

安全判断原则：

| 场景 | 是否更新 | 原因 |
|---|---|---|
| 非 native 归因 | ✅ 安全 | pick_source 通道不受时间影响 |
| native 归因，mapping 在订单之前创建 | ✅ 安全 | mapping 在订单发生前已存在 |
| native 归因，mapping 在订单之后创建 | ❌ 不安全 | 用事后创建的 mapping 反推历史订单是错的 |
| 无法判断时间关系 | ❌ 不安全 | 数据缺失保守处理 |

## 6. 归因结果写入（applyAttribution）

```java
// OrderAttributionReplayService.java:128-142
private void applyAttribution(ColonelsettlementOrder order, AttributionService.AttributionResult result) {
    order.setChannelUserId(result.channelUserId());
    order.setChannelDeptId(result.deptId());
    order.setUserId(result.userId());
    order.setDeptId(result.deptId());
    order.setColonelUserId(result.colonelUserId());
    order.setTalentId(result.talentId());
    if (StringUtils.hasText(result.activityId())) {
        order.setActivityId(result.activityId());
    }
    order.setAttributionStatus(result.attributionStatus());
    order.setAttributionRemark(result.attributionRemark());
    order.setUpdateTime(LocalDateTime.now());
    fillUserNames(order);  // 填充 userName（从 sys_user 表查）
}
```

## 7. 统计指标（ReplayResult）

```java
// OrderAttributionReplayService.java:171-182
public record ReplayResult(
        int scanned,           // 扫描的订单数
        int attributed,        // 归因成功的订单数
        int unattributed,      // 归因失败的订单数
        int updated,          // 实际写入的订单数（dry-run 时为 0）
        boolean dryRun,        // 是否干跑
        int nativeKeyMatched,        // native 通道命中的数量
        int safeToUpdate,            // 安全可更新的数量
        int unsafeBecauseCreatedAfterOrder,  // 因 mapping 晚于订单而不安全
        int colonelBuyinIdMismatch,  // colonelBuyinId 不匹配的数量
        int ambiguousMapping,        // 归因模糊的数量
        int stillUnattributed        // 最终仍未归因的数量
) {}
```

## 8. persistOrder 的双重写入

```java
// OrderAttributionReplayService.java:85
persistenceService.persistOrder(order);

// OrderSyncPersistenceService.java:37-56
// insertIgnoreByOrderId → 成功（新增）→ 写 PickSourceMapping + Merchant + 样品完成
//                     → 失败（已存在）→ updateSyncedById → 重新写 PickSourceMapping + Merchant + 样品完成
```

## 9. 典型使用场景

| 场景 | 调用方式 |
|---|---|
| 上线新的归因通道 | `replay(null, null, 200, false)` 全量重放 |
| 修复 NO_PICK_SOURCE 订单 | `replay(null, "NO_PICK_SOURCE", 200, false)` |
| 上线新 mapping 后修复特定订单 | `replay(["id1", "id2", ...], null, null, true)` 先 dry-run |
| 验证归因效果 | `replay(null, null, 50, true)` dry-run 查看 stats |

## 10. 相关概念

- [[DDD实战-团长SaaS系统/06-抖店开放平台集成]]（归因决策树）
- [[DDD实战-团长SaaS系统/10-商品运营与活动链路]]（PickSourceMapping 创建时间与安全更新）
- [[DDD实战-团长SaaS系统/16-操作审计日志体系]]（幂等写入模式）
