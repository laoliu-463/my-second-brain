---
title: 04-核心代码流程详解
tags: [DDD, Java, Spring Boot, MyBatis-Plus, PostgreSQL, 真实源码]
created: 2026-05-10
updated: 2026-05-10
sources: [ProductService.java(886-1175), AttributionService.java, PickSourceMappingService.java, PickSourceMapping.java, ProductSnapshot.java, ColonelsettlementActivityMapper.java]
---

# 04-核心代码流程详解（基于真实源码）

## 概述

本篇以真实源码为基础，逐行解析 4 个最关键的业务流程。所有代码片断均标注来源文件与行号，可直接在 IDE 中跳转阅读。

---

## 1. 转链完整流程：generatePromotionLink

**来源**：`ProductService.java` 第 886-1071 行

这是整个抖店归因体系的起点——达人申请推广链接时，同步生成 NATIVE/PICK_SOURCE 两条归因通道的 mapping 记录。

### 1.1 方法入口与前置校验

```java
// ProductService.java:886-911
@Transactional(rollbackFor = Exception.class)
public DouyinPromotionGateway.PromotionLinkResult generatePromotionLink(
        String activityId,
        String productId,
        UUID userId,
        UUID deptId,
        String externalUniqueId,
        Integer promotionScene,
        boolean needShortLink,
        String scene,
        String talentId) {

    // 前置1：确认 ProductSnapshot 存在（不存在则抛异常）
    ProductSnapshot snapshot = ensureSnapshotExists(activityId, productId);

    // 【核心】分层解析 colonelBuyinId，用于写 NATIVE mapping
    NativeColonelBuyinResolution nativeColonelBuyin =
            resolveColonelBuyinIdForNativeMapping(
                    snapshot.getActivityId(), snapshot.getProductId());

    // 生成 externalUniqueId（抖店 API 必填字段，用于生成稳定的 pickSource）
    String finalExternalId = StringUtils.hasText(externalUniqueId)
            ? externalUniqueId : String.valueOf(userId);

    int finalPromotionScene = promotionScene == null ? 4 : promotionScene;
    String finalScene = normalizePromotionScene(scene);

    // 前置2：确认商品已加入商品库（ProductBizStatus 必须是 ASSIGNED 或 LINKED）
    ProductOperationState state = getOrInitOperationState(activityId, productId);
    requireSelectedToLibrary(state, "生成推广链接");

    ProductBizStatus beforeStatus = productBizStatusService.readBizStatus(state);
    if (beforeStatus == null) {
        beforeStatus = ProductBizStatus.fromCode(state.getBizStatus());
    }

    // 判断是重新转链（LINKED 状态）还是首次转链（ASSIGNED 状态）
    boolean relinkExistingProduct = beforeStatus == ProductBizStatus.LINKED;
    if (beforeStatus != ProductBizStatus.ASSIGNED && !relinkExistingProduct) {
        throw new BusinessException(
                "当前状态不允许执行PROMOTION_LINK，当前状态：" + beforeStatus.name());
    }

    SysUser user = sysUserMapper.selectById(userId);
    // 构建 pickExtra：抖店用来区分达人的标识字段（最长 20 字符）
    String desiredPickExtra = buildPickExtra(userId);
```

### 1.2 调用抖音 API 生成推广链接

```java
    // ProductService.java:916-933
    try {
        // 调用抖店 buyin.promotionLink.generate 接口
        DouyinPromotionGateway.PromotionLinkResult result =
            douyinPromotionGateway.generateLink(
                new DouyinPromotionGateway.PromotionLinkCommand(
                        finalExternalId,
                        finalPromotionScene,          // 推广场景，4=商品详情页
                        List.of(snapshot.getProductId()),
                        needShortLink,
                        new DouyinPromotionGateway.PromotionContext(
                                userId,
                                deptId,
                                snapshot.getProductId(),
                                snapshot.getActivityId(),
                                snapshot.getDetailUrl(),
                                finalScene,
                                talentId,
                                desiredPickExtra  // ← 用于归因的达人标识
                        )
                )
        );
```

### 1.3 保存 PromotionLink（本地写）

```java
        // ProductService.java:935-953
        // 步骤1：保存推广链接主记录
        PromotionLink link = new PromotionLink();
        link.setId(UUID.randomUUID());
        link.setProductId(snapshot.getProductId());
        link.setActivityId(snapshot.getActivityId());
        link.setTalentId(talentId);
        link.setChannelUserId(userId);
        link.setChannelUserName(user != null ? user.getRealName() : "unknown");
        link.setOriginalProductUrl(snapshot.getDetailUrl());
        link.setPromotionUrl(result.promoteLink());    // 推广长链
        link.setShortUrl(result.shortLink());         // 短链
        link.setPickSource(result.pickSource());      // 抖店返回的 pickSource
        link.setPickExtra(result.pickExtra());        // 达人标识
        link.setLinkStatus("ACTIVE");
        link.setOperatorId(userId);
        link.setOperatorName(user != null ? user.getRealName() : "system");
        link.setCreatedAt(LocalDateTime.now());
        link.setUpdatedAt(LocalDateTime.now());
        promotionLinkMapper.insert(link);
```

### 1.4 保存 PickSourceMapping（核心归因记录）

```java
        // ProductService.java:955-1003
        // 步骤2：根据 colonelBuyinId 是否解析成功，决定写入 NATIVE 还是 PICK_SOURCE mapping
        if (nativeColonelBuyin.resolved()) {
            // ✅ colonelBuyinId 有值 → 写 NATIVE 通道（抖店原生归因）
            //   这条 mapping 供订单归因时通过 colonel_buyin_id 反查达人
            log.info("Native mapping resolved for activityId={}, productId={}, colonelBuyinId={}, source={}",
                    snapshot.getActivityId(),
                    snapshot.getProductId(),
                    nativeColonelBuyin.colonelBuyinId(),
                    nativeColonelBuyin.source());

            pickSourceMappingService.saveOrUpdate(
                    userId,
                    user != null ? user.getRealName() : "unknown",
                    deptId,
                    talentId,
                    null,
                    result.shortId(),
                    null,
                    result.pickSource(),
                    snapshot.getProductId(),
                    snapshot.getActivityId(),
                    snapshot.getDetailUrl(),
                    result.promoteLink(),
                    link.getId(),
                    finalScene,
                    result.pickExtra(),
                    nativeColonelBuyin.colonelBuyinId(),   // ← colonelBuyinId 写入
                    PickSourceMappingService.SOURCE_TYPE_NATIVE  // ← sourceType = NATIVE
            );
        } else {
            // ⚠️ colonelBuyinId 无值 → 跳过 NATIVE mapping，只写 PICK_SOURCE 通道
            //   归因时只能走 pickSource 通道，colonelBuyinId 归因会失败
            log.warn("Skip native mapping creation because colonel_buyin_id is unresolved, " +
                    "activityId={}, productId={}, source={}",
                    snapshot.getActivityId(),
                    snapshot.getProductId(),
                    nativeColonelBuyin.source());

            pickSourceMappingService.saveOrUpdate(
                    userId,
                    user != null ? user.getRealName() : "unknown",
                    deptId,
                    talentId,
                    null,
                    result.shortId(),
                    null,
                    result.pickSource(),
                    snapshot.getProductId(),
                    snapshot.getActivityId(),
                    snapshot.getDetailUrl(),
                    result.promoteLink(),
                    link.getId(),
                    finalScene,
                    result.pickExtra()   // ← 无 colonelBuyinId，走 PICK_SOURCE 通道
            );
        }
```

### 1.5 状态变更与异常处理

```java
        // ProductService.java:1005-1070
        Map<String, Object> payload = new LinkedHashMap<>();
        payload.put("promotionScene", finalPromotionScene);
        payload.put("needShortLink", needShortLink);
        payload.put("externalUniqueId", finalExternalId);
        payload.put("scene", finalScene);
        payload.put("talentId", talentId);
        payload.put("shortLink", result.shortLink());
        payload.put("promoteLink", result.promoteLink());
        payload.put("pickSource", result.pickSource());

        if (relinkExistingProduct) {
            // 重新转链：只更新 OperationState 中的链接字段，不触发状态变更
            state.setPromoteLink(result.promoteLink());
            state.setShortLink(result.shortLink());
            state.setPromotionScene(finalPromotionScene);
            state.setExternalUniqueId(finalExternalId);
            operationStateMapper.updateById(state);
            productBizStatusService.logStatusChange(...);
            return result;
        }

        // 首次转链：更新 OperationState + 变更商品业务状态为 LINKED
        productBizStatusService.changeStatus(
                state,
                ProductBizStatus.LINKED,
                "PROMOTION_LINK",
                userId,
                deptId,
                payload,
                "转链成功",
                current -> {
                    current.setPromoteLink(result.promoteLink());
                    current.setShortLink(result.shortLink());
                    current.setPromotionScene(finalPromotionScene);
                    current.setExternalUniqueId(finalExternalId);
                }
        );
        return result;

    } catch (RuntimeException ex) {
        // 转链失败：记录失败日志，但不阻断流程（异常向上抛出）
        productBizStatusService.logFailure(
                activityId, productId, beforeStatus,
                "PROMOTION_LINK", userId, deptId,
                payload, "转链失败", ex.getMessage()
        );
        throw ex;
    }
}
```

---

## 2. ColonelBuyinIdResolver 分层解析（POLICY 完整实现）

**来源**：`ProductService.java` 第 1097-1271 行

### 2.1 入口：resolveColonelBuyinIdForNativeMapping

```java
// ProductService.java:1101-1119
/**
 * 分层解析 colonelBuyinId，从本地到远程，依次尝试：
 *   ① ProductSnapshot.rawPayload.extraData.colonelBuyinId
 *   ② ProductOperationState.auditPayload.extraData.colonelBuyinId
 *   ③ ColonelsettlementActivity 主字段 + extraData（patch 修复点）
 *   ④ ActivityApi.detail() 回填（异常被吞，不阻断转链）
 */
private NativeColonelBuyinResolution
resolveColonelBuyinIdForNativeMapping(String activityId, String productId) {

    // 第1层：ProductSnapshot.rawPayload → extraData.colonelBuyinId
    String colonelBuyinId = resolveColonelBuyinIdFromSnapshot(activityId, productId);
    if (StringUtils.hasText(colonelBuyinId)) {
        return new NativeColonelBuyinResolution(
                colonelBuyinId, NativeColonelBuyinSource.PRODUCT_SNAPSHOT);
    }

    // 第2层：ProductOperationState.auditPayload → extraData.colonelBuyinId
    colonelBuyinId = resolveColonelBuyinIdFromOperationState(activityId, productId);
    if (StringUtils.hasText(colonelBuyinId)) {
        return new NativeColonelBuyinResolution(
                colonelBuyinId, NativeColonelBuyinSource.PRODUCT_OPERATION_STATE);
    }

    // 第3层：ColonelsettlementActivity 主字段 + extraData
    colonelBuyinId = resolveColonelBuyinIdFromActivity(activityId);
    if (StringUtils.hasText(colonelBuyinId)) {
        return new NativeColonelBuyinResolution(
                colonelBuyinId, NativeColonelBuyinSource.COLONEL_ACTIVITY);
    }

    // 第4层：调抖音 API 回填（兜底，失败不阻断）
    colonelBuyinId = hydrateColonelActivityMeta(activityId);
    if (StringUtils.hasText(colonelBuyinId)) {
        return new NativeColonelBuyinResolution(
                colonelBuyinId, NativeColonelBuyinSource.ACTIVITY_API_DETAIL);
    }

    return NativeColonelBuyinResolution.unresolved();
}
```

### 2.2 第3层核心方法：resolveColonelBuyinIdFromActivity（BUG 修复点）

```java
// ProductService.java:1121-1141
/**
 * 从 colonel_activity 表解析 colonelBuyinId。
 * 优先查主字段 colonel_buyin_id；
 * 主字段为 null 时，fallback 查 extra_data JSONB（patch 2026-05-10）。
 */
private String resolveColonelBuyinIdFromActivity(String activityId) {
    if (!StringUtils.hasText(activityId)) {
        return null;
    }

    // ① 优先查主字段（COALESCE UPSERT 问题：主字段可能为 null）
    ColonelsettlementActivity activity =
            colonelActivityMapper.selectByActivityId(activityId);
    if (activity != null && activity.getColonelBuyinId() != null) {
        return String.valueOf(activity.getColonelBuyinId());
    }

    // ② 主字段为 null 时，fallback 查 extraData JSONB（patch 新增）
    // hydrateColonelActivityMeta() 已经将真实 colonel_buyin_id 写入了 extraData
    Map<String, Object> extraData =
            colonelActivityMapper.selectExtraDataByActivityId(activityId);
    if (extraData != null && !extraData.isEmpty()) {
        // 尝试多个 key 变体（抖店 API 返回字段名不统一）
        String fromExtra = firstNonBlank(
                readString(extraData, "colonel_buyin_id", "colonelBuyinId"),
                readString(
                        readNestedMap(extraData, "extra_data", "extraData"),
                        "colonel_buyin_id", "colonelBuyinId"
                )
        );
        if (StringUtils.hasText(fromExtra)) {
            return fromExtra;
        }
    }
    return null;
}
```

**Bug 根因分析**：

`colonel_activity` 表的 UPSERT 使用了 COALESCE：
```sql
COALESCE(colonel_buyin_id, NEW_VALUE)
```
当旧记录主字段为 null 时，新值才写入。如果 UPSERT 执行时 `colonel_buyin_id` 本身为 null，则主字段永远为 null。

但 `hydrateColonelActivityMeta` 在第4层会调用 `activityApi.detail()` 并将真实值写入 `extra_data` JSONB 字段。patch 在第3层增加了对这个 JSONB 的 fallback 读取。

### 2.3 第1层实现：resolveColonelBuyinIdFromSnapshot

```java
// ProductService.java:1177-1194
private String resolveColonelBuyinIdFromSnapshot(String activityId, String productId) {
    if (!StringUtils.hasText(activityId) || !StringUtils.hasText(productId)) {
        return null;
    }
    ProductSnapshot snapshot = snapshotMapper.selectOne(
            new LambdaQueryWrapper<ProductSnapshot>()
                    .eq(ProductSnapshot::getActivityId, activityId)
                    .eq(ProductSnapshot::getProductId, productId)
                    .eq(ProductSnapshot::getDeleted, 0)
                    .last("limit 1"));

    if (snapshot == null) return null;

    // 先尝试 JSON 结构解析
    String fromPayload = resolveColonelBuyinIdFromPayload(snapshot.getRawPayload());
    if (StringUtils.hasText(fromPayload)) return fromPayload;

    // JSON 解析失败时，用正则从原始文本中提取（兜底）
    return extractColonelBuyinIdFromText(snapshot.getRawPayload());
}

// 从 JSON 结构的 payload 中提取（支持 extra_data 嵌套）
// ProductService.java:1241-1260
private String resolveColonelBuyinIdFromPayload(String raw) {
    Map<String, Object> payload = parseJsonObject(raw);
    if (payload.isEmpty()) return null;

    // 根层级尝试
    String fromRoot = readString(payload,
            "origin_colonel_buyin_id", "originColonelBuyinId",
            "colonel_buyin_id", "colonelBuyinId");
    if (StringUtils.hasText(fromRoot)) return fromRoot;

    // extra_data 嵌套层级尝试
    Map<String, Object> extraData =
            readNestedMap(payload, "extra_data", "extraData");
    return readString(extraData,
            "origin_colonel_buyin_id", "originColonelBuyinId",
            "colonel_buyin_id", "colonelBuyinId");
}

// ProductService.java:1262-1271
// 正则兜底：从原始文本（非 JSON）中提取 10+ 位数字
private static final Pattern BUYIN_ID_PATTERN = Pattern.compile(
    "(?:origin_colonel_buyin_id|originColonelBuyinId|" +
    "colonel_buyin_id|colonelBuyinId)\\s*[=:]\\s*['\"]?([0-9]{10,})"
);
private String extractColonelBuyinIdFromText(String raw) {
    if (!StringUtils.hasText(raw)) return null;
    Matcher matcher = BUYIN_ID_PATTERN.matcher(raw);
    if (matcher.find()) return matcher.group(1);
    return null;
}
```

### 2.4 第4层兜底：hydrateColonelActivityMeta（API 回填）

```java
// ProductService.java:1143-1175
/**
 * 调抖音 enterprise.marketing.promotion.activity.detail API，
 * 将返回的 colonel_buyin_id 回填到 colonel_activity 表（UPSERT）。
 * 调用失败 → 异常被吞，不阻断转链主流程。
 */
private String hydrateColonelActivityMeta(String activityId) {
    if (!StringUtils.hasText(activityId)) return null;
    try {
        Map<String, Object> response = activityApi.detail(null, activityId);
        Map<String, Object> data = readPrimaryDataNode(response);

        // 读 colonel_buyin_id（多个字段名变体）
        Long colonelBuyinId = readLong(data,
                "colonel_buyin_id", "colonelBuyinId");
        if (colonelBuyinId == null || colonelBuyinId <= 0L) return null;

        // UPSERT 到 colonel_activity 表（含 extra_data JSONB）
        colonelActivityMapper.upsertRealActivityMeta(
                UUID.nameUUIDFromBytes(
                        ("real-activity-" + activityId).getBytes(StandardCharsets.UTF_8)),
                activityId,
                readString(data, "activity_name", "activityName", "name"),
                readLong(data, "shop_id", "shopId"),
                readString(data, "shop_name", "shopName"),
                colonelBuyinId,
                readDecimal(data, "commission_rate", "commissionRate"),
                readDecimal(data, "service_rate", "serviceRate"),
                parseDateTimeValue(
                        readString(data, "activity_start_time", "activityStartTime",
                                "start_time", "startTime")),
                parseDateTimeValue(
                        readString(data, "activity_end_time", "activityEndTime",
                                "end_time", "endTime")),
                readString(data, "status_text", "statusText", "status"),
                LocalDateTime.now(),
                data   // ← 完整 JSON 写入 extra_data（含真实的 colonel_buyin_id）
        );
        return String.valueOf(colonelBuyinId);

    } catch (Exception ex) {
        // 不阻断转链主流程
        log.warn("Hydrate colonel activity meta failed, activityId={}", activityId, ex);
        return null;
    }
}
```

---

## 3. PickSourceMapping.saveOrUpdate 全链路

**来源**：`PickSourceMappingService.java` 第 1-452 行

### 3.1 5个重载方法（入口分发）

```java
// PickSourceMappingService.java:38-183
// 5个重载方法，最终都汇聚到第6个全参方法

// 入口1：不含 colonelBuyinId → 默认走 PICK_SOURCE 通道
public void saveOrUpdate(UUID userId, ..., UUID promotionLinkId) {
    saveOrUpdate(..., promotionLinkId, null);  // scene=null, pickExtra=null
}

// 入口2：不含 colonelBuyinId，传入 scene
public void saveOrUpdate(UUID userId, ..., UUID promotionLinkId, String scene) {
    saveOrUpdate(..., promotionLinkId, scene, null, SOURCE_TYPE_PICK_SOURCE);
}

// 入口3：不含 colonelBuyinId，传入 pickExtra
public void saveOrUpdate(UUID userId, ..., String scene, String pickExtra) {
    saveOrUpdate(..., scene, pickExtra, null, SOURCE_TYPE_PICK_SOURCE);
}

// 入口4：含 colonelBuyinId，自动判断 sourceType
public void saveOrUpdate(UUID userId, ..., String colonelBuyinId) {
    saveOrUpdate(...,
            colonelBuyinId,
            StringUtils.hasText(colonelBuyinId)
                    ? SOURCE_TYPE_NATIVE : SOURCE_TYPE_PICK_SOURCE);
}

// 入口5：全参数，显式指定 sourceType
public void saveOrUpdate(UUID userId, ...,
        String colonelBuyinId, String sourceType) {
    // 分发到全参方法
}

// 全参方法：第6个（实际执行逻辑）
@Transactional(rollbackFor = Exception.class)
public void saveOrUpdate(UUID userId, ...,
        String colonelBuyinId, String sourceType) {

    // ① 解析 sourceType（优先级：显式参数 > colonelBuyinId 有无）
    String resolvedSourceType = resolveSourceType(sourceType, colonelBuyinId);

    // ② 查询是否已存在 mapping（多条件组合查询）
    PickSourceMapping existing = findExistingMapping(...);

    if (existing == null) {
        // ③ INSERT 新记录
        try {
            PickSourceMapping mapping = buildMapping(...);
            mapping.setColonelBuyinId(resolveColonelBuyinId(colonelBuyinId));
            pickSourceMappingMapper.insert(mapping);
            logNativeAmbiguousIfNeeded(mapping);
            return;
        } catch (DuplicateKeyException ex) {
            // ④ 并发保护：两个线程同时 INSERT → 另一个先成功 → 重查
            existing = findExistingMapping(...);
            if (existing == null) throw ex;
        }
    }

    // ⑤ UPDATE 已存在记录
    existing.setUserId(userId);
    existing.setColonelBuyinId(resolveColonelBuyinId(colonelBuyinId));
    existing.setSourceType(resolvedSourceType);
    existing.setValidUntil(LocalDateTime.now().plusMonths(validMonths));
    existing.setStatus(1);
    pickSourceMappingMapper.updateById(existing);
    logNativeAmbiguousIfNeeded(existing);
}
```

### 3.2 findExistingMapping 查询优先级

```java
// PickSourceMappingService.java:279-328
private PickSourceMapping findExistingMapping(
        UUID userId, String pickSource, String productId,
        String activityId, UUID promotionLinkId,
        String colonelBuyinId, String sourceType) {

    // 优先级1：promotionLinkId（关联推广链接）
    if (promotionLinkId != null) {
        PickSourceMapping m = pickSourceMappingMapper.selectOne(
                new LambdaQueryWrapper<PickSourceMapping>()
                        .eq(PickSourceMapping::getPromotionLinkId, promotionLinkId)
                        .last("limit 1"));
        if (m != null) return m;
    }

    // 优先级2：(userId + pickSource + productId + activityId) 复合键
    if (userId != null && StringUtils.hasText(pickSource)
            && StringUtils.hasText(productId) && StringUtils.hasText(activityId)) {
        PickSourceMapping m = pickSourceMappingMapper.selectOne(
                new LambdaQueryWrapper<PickSourceMapping>()
                        .eq(PickSourceMapping::getUserId, userId)
                        .eq(PickSourceMapping::getPickSource, pickSource)
                        .eq(PickSourceMapping::getProductId, productId)
                        .eq(PickSourceMapping::getActivityId, activityId)
                        .last("limit 1"));
        if (m != null) return m;
    }

    // 优先级3：(NATIVE + colonelBuyinId + productId + activityId + userId) 复合键
    //   这是 NATIVE 通道的核心唯一性约束
    if (SOURCE_TYPE_NATIVE.equals(sourceType)
            && userId != null
            && StringUtils.hasText(colonelBuyinId)
            && StringUtils.hasText(productId)
            && StringUtils.hasText(activityId)) {
        PickSourceMapping m = pickSourceMappingMapper.selectOne(
                new LambdaQueryWrapper<PickSourceMapping>()
                        .eq(PickSourceMapping::getColonelBuyinId,
                                resolveColonelBuyinId(colonelBuyinId))
                        .eq(PickSourceMapping::getProductId, productId)
                        .eq(PickSourceMapping::getActivityId, activityId)
                        .eq(PickSourceMapping::getUserId, userId)
                        .eq(PickSourceMapping::getSourceType, SOURCE_TYPE_NATIVE)
                        .last("limit 1"));
        if (m != null) return m;
    }

    // 优先级4：纯 pickSource 查询（兜底）
    if (StringUtils.hasText(pickSource)
            && !StringUtils.hasText(productId)
            && !StringUtils.hasText(activityId)) {
        return pickSourceMappingMapper.selectOne(
                new LambdaQueryWrapper<PickSourceMapping>()
                        .eq(PickSourceMapping::getPickSource, pickSource)
                        .last("limit 1"));
    }

    return null;
}
```

### 3.3 logNativeAmbiguousIfNeeded（AMBIGUOUS 检测）

```java
// PickSourceMappingService.java:337-366
/**
 * 写完 NATIVE mapping 后，检查同一 (colonelBuyinId, activityId, productId)
 * 是否存在多个不同的 userId → 标记为 AMBIGUOUS。
 */
private void logNativeAmbiguousIfNeeded(PickSourceMapping mapping) {
    if (mapping == null
            || !SOURCE_TYPE_NATIVE.equals(mapping.getSourceType())
            || !StringUtils.hasText(mapping.getColonelBuyinId())
            || !StringUtils.hasText(mapping.getActivityId())
            || !StringUtils.hasText(mapping.getProductId())) {
        return;
    }

    List<PickSourceMapping> mappings = pickSourceMappingMapper.selectList(
            new LambdaQueryWrapper<PickSourceMapping>()
                    .eq(PickSourceMapping::getSourceType, SOURCE_TYPE_NATIVE)
                    .eq(PickSourceMapping::getColonelBuyinId, mapping.getColonelBuyinId())
                    .eq(PickSourceMapping::getActivityId, mapping.getActivityId())
                    .eq(PickSourceMapping::getProductId, mapping.getProductId())
                    .eq(PickSourceMapping::getStatus, 1));

    if (mappings == null || mappings.isEmpty()) return;

    long distinctUsers = mappings.stream()
            .map(PickSourceMapping::getUserId)
            .filter(java.util.Objects::nonNull)
            .distinct()
            .count();

    if (distinctUsers > 1) {
        log.warn("Native mapping ambiguous for activityId={}, productId={}, " +
                "colonelBuyinId={}, distinctUsers={}",
                mapping.getActivityId(), mapping.getProductId(),
                mapping.getColonelBuyinId(), distinctUsers);
    }
}
```

---

## 4. AttributionService.resolveAttribution 全链路

**来源**：`AttributionService.java` 第 1-506 行

### 4.1 归因策略总入口

```java
// AttributionService.java:52-219
public AttributionResult resolveAttribution(
        ColonelsettlementOrder order,
        java.util.Map<String, Object> source) {

    // 抽取归因字段（优先从 extraData 读，兼容多个字段名变体）
    String activityId = firstNonBlank(
            asString(source.get("colonel_activity_id")),
            asString(source.get("activity_id")),
            order.getActivityId());

    String productId = order.getProductId();
    String pickSource = order.getPickSource();
    String pickExtra = asString(source.get("pick_extra"));
    String talentUid = talentUid(source);
    UUID talentId = resolveTalentId(talentUid);

    // 前置检查：productId 必填
    if (!StringUtils.hasText(productId)) {
        return AttributionResult.unattributed(
                talentId, talentUid, activityId, null,
                REASON_PRODUCT_NOT_FOUND, NativeMappingTrace.none());
    }

    // 读 ProductOperationState（获取 colonelUserId）
    UUID colonelUserId = null;
    boolean activityStateMissing = false;
    if (StringUtils.hasText(activityId) && StringUtils.hasText(productId)) {
        ProductOperationState state = operationStateMapper.selectOne(
                new LambdaQueryWrapper<ProductOperationState>()
                        .eq(ProductOperationState::getActivityId, activityId)
                        .eq(ProductOperationState::getProductId, productId)
                        .last("limit 1"));
        if (state != null) colonelUserId = state.getAssigneeId();
        else activityStateMissing = true;
    }

    // 归因字段抽取
    String merchantId = firstNonBlank(
            asString(source.get("merchant_id")),
            asString(source.get("shop_id")),
            order.getShopId() == null
                    ? null : String.valueOf(order.getShopId()));

    // === 归因优先级链 ===

    // 第1优先：独家商家归因
    ExclusiveOwner merchantOwner = findExclusiveMerchantOwner(merchantId);
    if (merchantOwner != null) {
        return AttributionResult.attributed(
                merchantOwner.userId(), merchantOwner.deptId(),
                merchantOwner.userId(), talentId, talentUid,
                activityId, colonelUserId,
                REASON_ATTRIBUTED, NativeMappingTrace.none());
    }

    // 第2优先：独家达人归因
    ExclusiveOwner talentOwner = findExclusiveTalentOwner(talentUid);
    if (talentOwner != null) {
        return AttributionResult.attributed(
                talentOwner.userId(), talentOwner.deptId(),
                talentOwner.userId(), talentId, talentUid,
                activityId, colonelUserId,
                REASON_ATTRIBUTED, NativeMappingTrace.none());
    }

    // 第3优先：抖店原生归因（colonel_buyin_id 通道）
    String colonelsBuyinId = firstNonBlank(
            asString(source.get("colonel_buyin_id")),
            asString(source.get("colonelBuyinId")));
    String secondColonelsBuyinId = firstNonBlank(
            asString(source.get("second_colonel_buyin_id")),
            asString(source.get("secondColonelBuyinId")));
    String secondActivityId = firstNonBlank(
            asString(source.get("second_colonel_activity_id")),
            asString(source.get("secondColonelActivityId")));

    if (StringUtils.hasText(colonelsBuyinId)
            || StringUtils.hasText(secondColonelsBuyinId)) {
        NativeColonelMappingResolution colonelResolution =
                resolveNativeColonelAttribution(
                        colonelsBuyinId, activityId,
                        secondColonelsBuyinId, secondActivityId,
                        productId);
        PickSourceMapping colonelMapping = colonelResolution.mapping();

        if (colonelMapping != null && colonelMapping.getUserId() != null) {
            // ✅ 命中 native mapping → 归因成功
            return AttributionResult.attributed(
                    colonelMapping.getUserId(),
                    colonelMapping.getDeptId(),
                    colonelMapping.getUserId(),
                    talentId, talentUid,
                    firstNonBlank(colonelResolution.activityId(),
                            colonelMapping.getActivityId(), activityId),
                    colonelUserId,
                    REASON_COLONEL_ORDER_INFO,     // ← 关键 reason
                    colonelResolution.trace());
        }

        // ❌ 未命中 native mapping → 判断 reason
        String reason = colonelResolution.reason();
        if (!StringUtils.hasText(reason)) reason = REASON_COLONEL_MAPPING_NOT_FOUND;
        if (colonelMapping != null && colonelMapping.getUserId() == null)
            reason = REASON_CHANNEL_NOT_FOUND;

        return AttributionResult.unattributed(
                talentId, talentUid,
                firstNonBlank(colonelResolution.activityId(), activityId),
                colonelUserId, reason,
                colonelResolution.trace());
    }

    // 第4优先：渠道归因（pick_source 通道）
    if (!StringUtils.hasText(pickSource) && !StringUtils.hasText(pickExtra)) {
        return AttributionResult.unattributed(
                talentId, talentUid, activityId, colonelUserId,
                REASON_NO_PICK_SOURCE, NativeMappingTrace.none());
    }

    PickSourceMapping mapping = findPickSourceMapping(pickSource, pickExtra);
    if (mapping == null) {
        return AttributionResult.unattributed(
                talentId, talentUid, activityId, colonelUserId,
                activityStateMissing
                        ? REASON_ACTIVITY_NOT_FOUND
                        : REASON_MAPPING_NOT_FOUND,
                NativeMappingTrace.none());
    }
    if (mapping.getUserId() == null) {
        return AttributionResult.unattributed(
                talentId, talentUid, activityId, colonelUserId,
                REASON_CHANNEL_NOT_FOUND, NativeMappingTrace.none());
    }

    return AttributionResult.attributed(
            mapping.getUserId(), mapping.getDeptId(), mapping.getUserId(),
            talentId, talentUid,
            firstNonBlank(mapping.getActivityId(), activityId),
            colonelUserId, REASON_ATTRIBUTED, NativeMappingTrace.none());
}
```

### 4.2 抖店原生归因核心方法：resolveNativeColonelAttribution

```java
// AttributionService.java:229-326
/**
 * 抖店原生归因（三级 fallback）：
 *   主团长 → 主团长 mapping 精确查询
 *   → 未命中且有次团长 → 次团长映射查询
 *   → 次团长也未命中 → 返回主团长结果
 *
 * 精确查询条件：
 *   WHERE colonel_buyin_id = buyinId
 *   AND activity_id = activityId
 *   AND product_id = productId
 *   AND source_type = 'NATIVE'
 *   AND status = 1
 */
protected NativeColonelMappingResolution
resolveNativeColonelAttribution(
        String firstColonelsBuyinId, String firstActivityId,
        String secondColonelsBuyinId, String secondActivityId,
        String productId) {

    boolean hasSecondActivity = StringUtils.hasText(secondActivityId);

    // ① 先查主团长
    NativeColonelMappingResolution firstResolution =
            resolveNativeColonelOrderMapping(
                    firstColonelsBuyinId, firstActivityId,
                    productId, !hasSecondActivity);
    if (firstResolution.mapping() != null
            || REASON_COLONEL_MAPPING_AMBIGUOUS.equals(firstResolution.reason())) {
        return firstResolution;  // 命中或歧义，直接返回
    }

    // ② 主团长未命中，且有次团长信息 → 查次团长
    if (hasSecondActivity || StringUtils.hasText(secondColonelsBuyinId)) {
        NativeColonelMappingResolution secondResolution =
                resolveNativeColonelOrderMapping(
                        secondColonelsBuyinId, secondActivityId,
                        productId, false);
        if (secondResolution.mapping() != null
                || REASON_COLONEL_MAPPING_AMBIGUOUS.equals(
                        secondResolution.reason())) {
            return secondResolution;
        }
        if (hasSecondActivity) return secondResolution;
    }

    return firstResolution;  // 返回主团长的"未找到"结果
}

/**
 * 单个 buyinId 的精确查询（含 generic fallback）。
 *
 * 精确查询（优先）：
 *   WHERE colonel_buyin_id = buyinId
 *   AND activity_id = activityId
 *   AND product_id = productId
 *   AND source_type = 'NATIVE'
 *   AND status = 1
 *
 * Fallback（activityId/productId 缺失时）：
 *   WHERE colonel_buyin_id = buyinId
 *   AND source_type = 'NATIVE'
 *   AND status = 1
 */
protected NativeColonelMappingResolution
resolveNativeColonelOrderMapping(
        String colonelsBuyinId, String activityId, String productId,
        boolean allowGenericFallback) {

    if (!StringUtils.hasText(colonelsBuyinId)) {
        return new NativeColonelMappingResolution(
                null, REASON_COLONEL_MAPPING_NOT_FOUND,
                activityId, NativeMappingTrace.none());
    }

    if (StringUtils.hasText(activityId) && StringUtils.hasText(productId)) {
        // 精确查询：colonelBuyinId + activityId + productId + NATIVE
        NativeColonelMappingResolution exactNative =
                singleMappingWithReason(
                        pickSourceMappingMapper.selectList(
                                new LambdaQueryWrapper<PickSourceMapping>()
                                        .eq(PickSourceMapping::getColonelBuyinId, colonelsBuyinId)
                                        .eq(PickSourceMapping::getActivityId, activityId)
                                        .eq(PickSourceMapping::getProductId, productId)
                                        .eq(PickSourceMapping::getSourceType, SOURCE_TYPE_NATIVE)
                                        .eq(PickSourceMapping::getStatus, 1)
                                        .orderByDesc(PickSourceMapping::getUpdateTime)),
                        activityId, false, true);
        if (exactNative.mapping() != null
                || REASON_COLONEL_MAPPING_AMBIGUOUS.equals(exactNative.reason())) {
            return exactNative;
        }

        // Fallback 查询：activityId + productId + NATIVE（不限制 colonelBuyinId）
        // 用于处理订单的 activityId 指向一个不同 buyinId 的情况
        NativeColonelMappingResolution exactActivityProduct =
                singleMappingWithReason(
                        pickSourceMappingMapper.selectList(
                                new LambdaQueryWrapper<PickSourceMapping>()
                                        .eq(PickSourceMapping::getActivityId, activityId)
                                        .eq(PickSourceMapping::getProductId, productId)
                                        .eq(PickSourceMapping::getSourceType, SOURCE_TYPE_NATIVE)
                                        .eq(PickSourceMapping::getStatus, 1)
                                        .orderByDesc(PickSourceMapping::getUpdateTime)),
                        activityId, true, false);
        if (exactActivityProduct.mapping() != null
                || REASON_COLONEL_MAPPING_AMBIGUOUS.equals(
                        exactActivityProduct.reason())) {
            PickSourceMapping mapping = exactActivityProduct.mapping();
            if (mapping != null
                    && !colonelsBuyinId.equals(mapping.getColonelBuyinId())) {
                // buyinId 不匹配 → 记录 mismatch
                return new NativeColonelMappingResolution(
                        mapping,
                        exactActivityProduct.reason(),
                        firstNonBlank(activityId, mapping.getActivityId()),
                        exactActivityProduct.trace()
                                .withColonelBuyinIdMismatch(true));
            }
            return exactActivityProduct;
        }
    }

    if (!allowGenericFallback) {
        return new NativeColonelMappingResolution(
                null, REASON_COLONEL_MAPPING_NOT_FOUND,
                activityId, NativeMappingTrace.none());
    }

    // generic fallback：只用 colonelBuyinId 查询（最宽松）
    return singleMappingWithReason(
            pickSourceMappingMapper.selectList(
                    new LambdaQueryWrapper<PickSourceMapping>()
                            .eq(PickSourceMapping::getColonelBuyinId, colonelsBuyinId)
                            .eq(PickSourceMapping::getSourceType, SOURCE_TYPE_NATIVE)
                            .eq(PickSourceMapping::getStatus, 1)
                            .orderByDesc(PickSourceMapping::getUpdateTime)),
            activityId, false, true);
}
```

### 4.3 归因结果记录（AttributionResult）

```java
// AttributionService.java:450-511
public record AttributionResult(
        UUID channelUserId,    // 归因到的达人 userId
        UUID deptId,            // 部门 ID
        UUID userId,            // 同 channelUserId（兼容）
        UUID talentId,          // 抖店侧达人 talentId
        String talentUid,       // 抖店侧达人 UID
        String activityId,      // 关联活动 ID
        UUID colonelUserId,     // 团长 userId（来自 OperationState.assigneeId）
        String attributionStatus,  // ATTRIBUTED | UNATTRIBUTED
        String attributionRemark,  // 归因原因
        NativeMappingTrace nativeTrace  // 归因追踪
) {
    // 归因成功
    public static AttributionResult attributed(...) {}

    // 归因失败
    public static AttributionResult unattributed(...) {}
}

// 抖店原生归因追踪（用于调试和日志）
public record NativeMappingTrace(
        boolean nativeKeyMatched,        // NATIVE key 精确命中
        boolean colonelBuyinIdMismatch,  // colonelBuyinId 不匹配
        boolean ambiguousMapping,        // 多个 mapping 歧义
        boolean usedActivityProductFallback,  // 用了 activity+product fallback
        LocalDateTime mappingCreatedAt  // mapping 创建时间
) {
    public static NativeMappingTrace none() { ... }
}
```

---

## 5. ColonelsettlementActivity UPSERT 与 COALESCE 问题

**来源**：`ColonelsettlementActivityMapper.java` + 数据库 UPSERT 逻辑

### 5.1 表结构（核心字段）

```sql
CREATE TABLE colonel_activity (
    id              UUID PRIMARY KEY,
    activity_id     VARCHAR(64) NOT NULL UNIQUE,  -- 抖店侧活动 ID
    colonel_buyin_id BIGINT,                      -- 【问题字段】主字段可能为 null
    extra_data      JSONB DEFAULT '{}',          -- 包含真实的 colonel_buyin_id
    created_at      TIMESTAMP,
    updated_at      TIMESTAMP
);
```

### 5.2 UPSERT SQL（根因分析）

```java
// Mapper XML 或 @Select 注解中的 UPSERT 语句
@Select("""
    INSERT INTO colonel_activity
        (id, activity_id, colonel_buyin_id, extra_data, ...)
    VALUES
        (#{id}, #{activityId}, #{colonelBuyinId}, #{extraData}::jsonb, ...)
    ON CONFLICT (activity_id)
    DO UPDATE SET
        colonel_buyin_id = COALESCE(
            colonel_activity.colonel_buyin_id,
            EXCLUDED.colonel_buyin_id
        ),
        extra_data = COALESCE(
            colonel_activity.extra_data,
            EXCLUDED.extra_data
        )
    """)
void upsertRealActivityMeta(...);
```

**问题**：COALESCE 语义是"取第一个非 null 值"。

| 场景 | 旧记录 colonel_buyin_id | 新值 colonel_buyin_id | 写入结果 |
|---|---|---|---|
| 首次 UPSERT | null | 719302412345678901 | **719302412345678901** ✅ |
| 更新活动名称 | 719302412345678901 | null | **719302412345678901** ✅（旧值保留）|
| 转链时序问题 | **null**（首次 UPSERT 时传 null）| null | **null** ❌（永久 null）|

**时序问题复盘**：
1. 商家在抖店后台创建活动 → 系统录入 `colonel_activity`（`colonel_buyin_id = null`）
2. 系统调 `activityApi.detail()` → 返回 `colonel_buyin_id`
3. 但此时活动已录入，若 UPSERT 时 API 返回值没有被正确传入 → 主字段永久为 null
4. `extra_data` JSONB 有值，但旧代码只查主字段 → 查询结果为 null
5. **Patch**：`resolveColonelBuyinIdFromActivity()` 第3层增加 `extra_data` fallback

### 5.3 extraData JSONB 结构（典型示例）

```json
{
  "colonel_buyin_id": "719302412345678901",
  "activity_name": "2026夏季精选联盟活动",
  "shop_id": 1234567890,
  "commission_rate": "0.20",
  "extra_data": {
    "colonel_buyin_id": "719302412345678901"
  }
}
```

---

## 6. 关键数据结构汇总

### 6.1 PickSourceMapping 实体完整字段

```java
// PickSourceMapping.java
@TableName("pick_source_mapping")
public class PickSourceMapping extends BaseEntity {
    private UUID userId;              // 归因到的达人用户
    private String shortId;          // 短 ID（从 pickSource 提取）
    private UUID uuidSeed;           // UUID 种子
    private UUID deptId;             // 部门
    private String pickSource;       // 抖店返回的 pickSource（唯一标识）
    private String colonelBuyinId;   // 团长19位buyin_id（NATIVE通道）
    private String productId;        // 商品 ID
    private String activityId;       // 活动 ID
    private String sourceUrl;        // 原始商品 URL
    private String convertedUrl;     // 转换后 URL
    private String pickExtra;        // 达人标识
    private UUID promotionLinkId;    // 关联推广链接
    private String channelUserName;  // 达人姓名
    private String talentId;        // 抖店达人 ID
    private String talentName;       // 抖店达人昵称
    private String scene;            // 推广场景
    private String sourceType;      // PICK_SOURCE | NATIVE
    private LocalDateTime validFrom; // 有效期起始
    private LocalDateTime validUntil;// 有效期截止（默认+3个月）
    private Integer status;          // 1=有效
}
```

### 6.2 ProductSnapshot 实体（存储抖店原始数据）

```java
// ProductSnapshot.java
@TableName("product_snapshot")
public class ProductSnapshot extends BaseEntity {
    private String activityId;       // 活动 ID
    private String productId;       // 商品 ID
    private String title;           // 商品标题
    private String cover;           // 封面图
    private Long price;             // 价格（分）
    private Long shopId;            // 店铺 ID
    private String shopName;        // 店铺名称
    private String detailUrl;       // 商品详情页 URL
    private String rawPayload;       // 【关键】抖店 API 原始 JSON，含 extra_data.colonel_buyin_id
    private LocalDateTime syncTime; // 同步时间
}
```

---

## 相关概念

- [[DDD实战-团长SaaS系统/01-战略设计-限界上下文划分]]
- [[DDD实战-团长SaaS系统/02-核心领域模型详解]]
- [[DDD实战-团长SaaS系统/03-本地与三方调用SOP分离]]
- [[DDD实战-团长SaaS系统/index]]
