自我介绍
面试官您好，我叫曹家宁，目前就读于大连交通大学软件工程本科二年级，求职方向是 Java 后端开发实习生。

在校期间，我系统学习了 Java 基础、集合、异常、泛型、反射和 Stream，并掌握了线程池、乐观锁、悲观锁等基础并发控制方法。

我熟悉 Spring Boot、MyBatis/MyBatis-Plus 等后端框架，以及 Spring Cloud Alibaba 生态中的 Nacos、Gateway、OpenFeign、Sentinel、Seata，具备 RESTful 接口开发和调试能力。

数据库方面，我熟悉 MySQL、PostgreSQL，了解索引使用、事务和基础 SQL 优化思路。

项目方面，我做过两个完整项目：一是基于 RAG 的 AI 简历分析系统，涉及 Spring AI、PostgreSQL + pgvector、Redis Stream，实现简历解析、向量检索和 AI 问答；二是教务管理系统，基于 Spring Cloud Alibaba 微服务架构，实现学生、课程和消课管理模块。

我希望通过实习参与真实业务项目，提升后端开发能力和系统设计能力。

AI面试项目

RAG 是 Retrieval-Augmented Generation，也就是检索增强生成。在我的项目里，用户上传简历后，系统会先解析 PDF 或 DOCX 文件，将文本清洗并按段落或固定长度切分成多个 chunk。

每个 chunk 会通过 embedding 模型生成向量，然后存入 PostgreSQL + pgvector 中，方便后续做向量相似度检索。

当用户提出问题时，系统会先将问题向量化，然后检索最相关的简历片段，再把这些内容和问题一起作为 Prompt 输入大模型生成回答。

这样，AI 的回答不仅精准，而且基于用户实际简历内容，避免了胡说。

在项目中，用户上传简历后，会有几个耗时操作，比如文件解析、文本切分、向量化以及报告生成。

如果这些步骤都同步执行，接口响应会很慢，用户体验不好。

所以我使用 Redis Stream，把这些任务放到异步队列里处理。消费者异步消费任务，完成向量化和报告生成，同时不阻塞用户请求。

我之所以选择 Redis Stream 而不是专门的消息队列，是因为项目本身已经使用 Redis，它轻量且足够处理中小规模异步任务，减少了额外组件复杂度。
